{
	"name": "delta",
	"version": "2.0.0",
	"description": "Document Editing Logistics Technical Aid",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node src/bot.js",
		"build": "docker build -t %npm_package_name%/alpine:%npm_package_version% .",
		"container": "docker run -d --restart unless-stopped --name %npm_package_name% %npm_package_name%/alpine:%npm_package_version%"
	},
	"keywords": [],
	"author": "Vio",
	"license": "ISC",
	"dependencies": {
		"delta-google-spreadsheet": "^3.1.16",
		"discord.js": "^14.11.0",
		"dotenv": "^8.2.0",
		"googleapis": "^39.2.0"
	},
	"devDependencies": {
		"eslint": "^7.22.0"
	}
}
